[package]
name = "rust-cli"
version = "0.1.0"
description = "Rust CLI application with clap / clap omplete"
min_forge_version = "0.4.0"

[author]
name = "what386"

[requires]
programs = ["git", "cargo"]
